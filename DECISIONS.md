# SwartzNet Rebuild — Decision Log

> The running decision log the rebuild playbook requires: every assumption made on the
> author's behalf, every question still open, and where the full rationale lives. This file
> consolidates; the detailed records are authoritative:
>
> - `SPEC.md` §7 — all 81 open questions, with `file:line` evidence anchors
> - `ARCHITECTURE.md` § "Assumptions & deferred questions" — the answered defaults in context
> - `docs/rebuild/phase3-design-record.md` — the three candidate architectures, judge scoring, critic fixes
> - `docs/rebuild/phase1-verification-ledger.md` — per-claim adversarial verification of SPEC §6 (135 confirmed / 13 refuted)

## Phase state

Phases 0–3 complete (artifacts committed 2026-07-09). **The Phase-3/4 gate CLEARED on
2026-07-17**: the author confirmed all five SPEC §0 checklist items unamended and approved
`ARCHITECTURE.md` + `PLAN.md`. Phase 4 is in progress, starting at Slice 0.

## Process decisions

| # | Decision | Rationale |
|---|---|---|
| P1 | Legacy preserved on the **`legacy-snapshot`** branch (pushed), not a `legacy/` directory | Keeps full history and diffability; the working branch stays a buildable Go module with stable import paths |
| P2 | The playbook's `DESIGN.md` is delivered as **`ARCHITECTURE.md`** (design) + **`PLAN.md`** (14-slice build order) | Two audiences, one contract; where they disagree the architecture wins and the plan is corrected |
| P3 | Every SPEC §6 defect claim was adversarially verified before being trusted; **refuted claims must NOT be "fixed"** in the rebuild | Several plausible-looking "bugs" (e.g. PoW=0 minting, seeded heavy-tail rule) are intentional, documented design — ledger in `docs/rebuild/phase1-verification-ledger.md` |
| P4 | Separation of new vs old code is **by branch, not by directory**: the rebuild replaces the tree on the rebuild branch while `legacy-snapshot` preserves the reference | Wire/disk compatibility is the deliverable; parallel trees would fork the module path, `dist/` workflow, and CI for no isolation gain |
| P5 | Same stack retained: Go 1.24.1, anacrolix/torrent v1.61.0, Bleve v2, Fyne v2 | SPEC §5.4's quirk catalog and the wire-compat matrix are anacrolix-specific and interop-tested; a stack change would invalidate the behavioral spec's anchors |
| P6 | Legacy tests that pin *buggy* behavior (permissive admission, static services mask, sequential search, unattributed ingest) are **re-derived, not ported** | Porting them would freeze the §6 defects the rebuild exists to remove |

## Assumed defaults (recorded on the author's behalf — override any of these)

Each maps a SPEC §7 question to the default the architecture adopts. Full context in
`ARCHITECTURE.md` § "Assumptions & deferred questions".

| §7 ref | Decision | Rationale |
|---|---|---|
| A5 | Services mask always-OR of `DefaultServices()` is a **bug**; `capability.Announced()` is the sole, live producer | Downgrades must reach the wire or ShareLocal=0 nodes advertise capabilities they reject |
| A3 | Unknown/future service bits are **ignored, never rejected** | Doc 06's masking wording is self-contradictory; ignore-unknown is the only forward-compatible reading |
| A1/A2 | Keep the code's **reject code 2** on scope mismatch (and its reuse for sync refusal) for wire compat; flag the BEP draft for a future codepoint | Interop with existing peers beats doc fidelity; the spec is fixable, shipped bytes are not |
| H46 | One unsafe gate: **`SWARTZNET_UNSAFE=1`** (+ `testing.Testing()` for testlab), documented | The two-name undocumented sequence broke the project's own testbed |
| B8/B9 | Admission is **deny-by-default**; permissive policy is test-only; a curated `seeds.json` is a **release prerequisite, not code** | The legacy placeholder admitted any unknown pubkey — inverted its own documented default |
| C12–C17 | Legacy per-keyword BEP-44 stays the shipping default; Aggregate lives behind the `RecordBackend` seam, **off by default**; retirement/PoW-ramp/salt schedule is an operator decision | ≥12-month legacy-read contract is binding; migration becomes adapter selection, not re-architecture |
| C16 | `MostDistinctive` is the **only** lookup-token chooser, now | The first-token lookup is a verified defect ("new ubuntu" queried the DHT for "new") |
| D20 | Bad-signature torrents **still add** (empty `SignedBy`, `ErrBadSignature` logged) | Blocking would fork behavior from vanilla and punish benign re-shares; UIs distinguish benign vs tamper |
| D21 | Trusted publishers **are exempt** from flag demotion | The trust package documents the exemption; the legacy just never checked it |
| D22 | Download completion does **not** auto-`RecordConfirmed` | Avoids self-reinforcing reputation; confirm stays an explicit user signal, consistent across surfaces |
| D23 | Bloom + reputation get a **bounded periodic checkpoint** (~5 min + batch flush) in addition to save-on-close | Crash loses at most one interval instead of the whole session's spam-resistance signal |
| D25 | identity.key permission gate stays **exactly 0600** (0400 rejected) | Preserves the legacy's observable contract; loosening is a one-line change if the author prefers "no group/other bits" |
| E27 | Queue promotion is **decoupled** from the Bloom gate | A nil Bloom stranding a completed torrent's slot is a verified defect, not a design |
| E28 | Per-torrent indexing-off **still publishes existence** to Layer D; only `--no-index`/`--no-dht-publish` suppress publication | Matches the code comment's stated privacy posture; flagged for the author under §7-28 |
| F36 | Index deletion becomes a reachable **"Forget"** action; downloaded files kept by default | Removed-but-searchable-forever is a verified defect; file deletion stays a separate, explicit act |
| §7-Q37 | Local + companion docs share one ID namespace, last-write-wins, **except non-empty `SignedBy` is sticky** against unsigned overwrites | Keeps the companion-attribution fix from being clobbered by a later local magnet add |
| §7-Q40 | Raw `.html` stays indexed by the **plaintext** extractor with tags left in; no standalone HTML extractor | "Improving" this silently changes the indexed content of every `.html` file |
| G43 | PeerBook state does **not** persist across restarts (v1 scope) | Matches legacy behavior; AddrMan-style persistence is deferred, recorded, and additive later |
| I51 | `add`-as-daemon stays (no `serve` subcommand in the final CLI; Slice 0's `serve` is a temporary scaffold deleted in Slice 2); bare 40-hex accepted as infohash add | Preserves the legacy UX contract; the scaffold exists only because Slice 0 has no engine to `add` |
| I53/I54 | `Options.PublisherPubKey` wired from identity; `/capabilities` mutates **only** `Sharing` with PATCH semantics | The daemon-owned Publisher bit becomes unreachable from the setter — the clobber becomes unrepresentable |
| J60 | User-facing license string: **Apache-2.0** (single build-stamped source) | `LICENSE`, README, and `THIRD_PARTY_LICENSES` all say Apache-2.0; the GUI About string was drift |
| §2.9/§5.9 | Non-loopback API bind **warns loudly and binds** (one-time "API is UNAUTHENTICATED" warning), not a hard refusal | Matches the spec's warn-and-bind; invariant #3's guarantee is *no auth concept in httpapi*, not a bind restriction |

## Resolved — the author gate (2026-07-17)

1. **`SPEC.md` §0 confirmed** — all five checklist items (mission framing, Aggregate
   endgame, scope exclusions, the four invariants, design-center user), unamended. §0 is
   now the normative statement of intent.
2. **`ARCHITECTURE.md` + `PLAN.md` approved** — implementation authorized, starting at
   Slice 0 (walking skeleton).

## Open — deliberately deferred (does not block Phase 4)

- **A1/A2** — BEP normativity of scope-reject vs downgrade; a dedicated sync reject code.
- **B8** — who curates/distributes the production `seeds.json` trust root (hard release gate; `/aggregate` admission counts make a starved node observable meanwhile).
- **C12–C17** — PPMI retirement schedule, PoW ramp mechanics, salt convergence.
- **C18** — how signed metainfo reaches BEP-9-only fetchers (`ut_signature` vs HTTPS mirror vs in-info signing); deferred while Channel-B crawl is non-load-bearing.
- **D19** — `next_pk` key rotation: reserved, unused.
- **G43** — PeerBook persistence (intentional v1 scope).
- The long tail of §7 (ops/docs/CI questions, §7-K/L/M) — resolved during Slice 13 or as encountered; every resolution gets appended here.

## Slice 0 decisions (2026-07-17)

| # | Decision | Rationale |
|---|---|---|
| S0-1 | §7-Q48 resolved: `Validate()` runs every pure rejection (empty DataDir, port range, unsafe gate) **before** either `MkdirAll` | A rejected config must leave the filesystem untouched (fail-closed ethos); error strings and the create-set are unchanged; pinned by test |
| S0-2 | `GET /healthz` ships in Slice 0 with an **instance-scoped** version (field on `httpapi.Options`), not the legacy process-global atomic | SPEC §5.9 flags the global as a wart; observable JSON is identical; deliberate §5 deviation, recorded |
| S0-3 | Middleware order is the legacy `bodylimit ⊃ CSRF ⊃ mux`; ARCHITECTURE.md's reversed prose corrected | SPEC §5.9 and the legacy code agree; the cap only swaps `r.Body`, so a CSRF-403 never reads the body |
| S0-4 | `serve --api-addr ""` runs an API-less daemon (empty-path = feature-off); a *failed* bind exits 1 (`swartznet: http api did not start`) after the daemon's degraded-start warning | Distinguishes "requested none" from "requested but failed"; `daemon.New` itself stays degrade-not-abort per SPEC §2.1 |
| S0-5 | `status` unreachable-daemon hint says `start it with: swartznet serve` until Slice 2 restores the legacy `swartznet add <magnet>` | The hinted command must exist; first line stays byte-identical to legacy |
| S0-6 | Version placeholder is `v0.9.0-dev`; release stamping stays `-ldflags "-X main.Version=..."` | Rebuild binaries must be distinguishable from the legacy v0.8.0 line |
| S0-7 | `newLogger`/`signalContext`/`reportRunErr` stay in `package main` until the GUI slice needs to share them | No new module outside the ARCHITECTURE map; sharing lands with its second consumer |
| S0-8 | `SWARTZNET_LOG` gains an explicit `"info"` case and a Warn on unrecognized non-empty values (§7-Q50: yes, warn); unknown values still run at Info | Behavior-compatible with legacy's silent fall-through, but observable; values remain case-sensitive |
| S0-9 | `config` has no `NoIndex` field yet; the daemon's NoIndex→Cfg mirroring line (SPEC §5.8) lands in Slice 2 with the engine that consumes it | Dead fields invite drift; the rule is pinned by SPEC/PLAN and gets its regression test with a consumer |
| S0-10 | `status --json` keeps the legacy `{"status":...}` envelope but skips the best-effort `/aggregate` fetch until that slice | Output is byte-identical to legacy-against-an-aggregate-less daemon; one less dead request |
| S0-11 | `httpapi.NewWithOptions` keeps the legacy empty-addr → `localhost:7654` constructor fallback even though the daemon treats empty as disabled | Legacy-pinned constructor contract; the daemon-level empty=off semantics are what users observe |
| S0-12 | Teardown log vocabulary frozen: `daemon.close_begin` → `daemon.bg_joined` → `httpapi.stopped` → `daemon.close_done` (new surface — legacy logged no teardown) | PLAN DoD requires reverse-order teardown observable in logs; later slices extend the same dotted style |
| S0-13 | SPEC §2.1's `./swartznet-data` fallback corrected to the code-true `./swartznet-state` (was §6.3-flagged drift) | Code behavior is the spec of record for the share-root fallback |
| S0-14 | The GUI binary is deliberately not rebuilt until the GUI slice; `dist/swartznet-gui-dev-linux-amd64` still contains the **legacy** build (as does `dist/swartznet-legacy-v0.8.0`, kept on purpose) | The rebuilt tree has no GUI source yet; the per-change both-binaries rule resumes at Slice 11 |
| S0-15 | Adversarial-review fixes: `goBG` registration is teardown-aware (no Add/Wait race, no-op after Close); double `Start` errors instead of orphaning the first listener; `cmd_status` non-200/decode output restored to byte-identical legacy shape; legacy `//go:embed index.html static/*` pattern kept; the per-keyword status table ported now (renders against a legacy daemon); a real-signal-path test (`syscall.Kill` SIGINT/SIGTERM → 130) guards the DoD; `scripts/dod-slice0.sh` checked in so the binary DoD run is reproducible | Each was a review finding with a concrete failure scenario; fixes verified by new pinning tests |

## Slice 1 decisions (2026-07-18)

| # | Decision | Rationale |
|---|---|---|
| S1-1 | `identity.Load(path string, allowCreate bool) (*Identity, error)` — the caller computes `allowCreate`; the redundant `isDefaultPath` return in ARCHITECTURE's original sketch is dropped (ARCHITECTURE amended) | The stdlib-only identity leaf cannot resolve the XDG default itself; enforcement stays in one place (Load's create branch), closing the legacy stat-then-create TOCTOU |
| S1-2 | Auto-create rule is **path-based**: the daemon compares `Cfg.IdentityPath == config.Default().IdentityPath`; `--identity` naming the default path behaves like the default | The daemon cannot know flag provenance; a user explicitly typing the default path and getting first-run creation is unsurprising |
| S1-3 | Explicit `--identity` load failure is **fatal** at `serve` (exit 1, `swartznet: identity did not load`); default-path failure **degrades** (warn + publisher-less) per SPEC §2.8 | The user explicitly named a key — running without it contradicts intent; SPEC pins only the default-path (non-fatal) case |
| S1-4 | The four legacy identity error strings preserved byte-verbatim; new load-only-missing error wraps `os.ErrNotExist` | Operator tooling and tests may match on them; `errors.Is` compatibility retained |
| S1-5 | Log events renamed `engine.identity_loaded/…_load_err` → `daemon.identity_loaded/…_load_err`, attr keys `pubkey`/`err` kept | Ownership moved from engine to daemon per ARCHITECTURE; the attrs are the observable contract |
| S1-6 | Creation: plain `WriteFile` + explicit `Chmod(0600)` after (no O_EXCL, no tmp+rename) | The chmod defeats exotic-umask bricking (e.g. 0277 → 0400 file → next load rejected); torn writes already fail closed on the size gate; atomicity is spec-mandated for trust.json/.torrent, not identity.key |
| S1-7 | Parent-dir `MkdirAll(0700)` moved to the **create arm** only; a parent-is-a-file path now surfaces the raw ENOTDIR stat error instead of legacy's `identity: mkdir` error | A refused load must have zero side effects (same principle as S0-1); pinned by test |
| S1-8 | Legacy dead branch "does not contain a valid ed25519 key" dropped; symlink semantics stay `os.Stat` (follow); no Windows perm carve-out yet — the exact-0600 gate makes identity unloadable on Windows exactly as legacy shipped, flagged under §7 open questions | Rebuild-faithful; a Windows carve-out is a deliberate future decision, not a silent divergence |
| S1-9 | `/status` `publisher.pubkey` renders whenever the probe is non-nil — deliberately un-nested from any publisher collaborator (legacy nested it, half of the §6 defect) | Slice 1 has no publisher; nesting would make the DoD unachievable and re-hide the pubkey for leech-only nodes later |
| S1-10 | `httpapi.Options.PublisherPubKey` stays `func() string` (legacy shape), not a plain string | Leaves room for key rotation (`next_pk`, §7-D19) without an Options change |
| S1-11 | Adversarial-review fixes: `allowCreate` compares `filepath.Clean`ed paths (a `/./`-spelled default still auto-creates, pinned by test); `swartznet help` and `serve -h` now agree on the path-based `--identity` wording; a hostile-umask (0277) unit test pins the post-create Chmod; `TestServeBadConfig` gained structural XDG isolation; the DoD script got a `timeout` on the fatal-exit check, readiness polling instead of a fixed sleep, and python3 instead of xxd; ARCHITECTURE/PLAN stale `IsDefaultPath` wording amended; the Windows perm-gate corollary recorded in SPEC §7-Q25 | Each was a review finding with a concrete scenario; fail-closed behavior unchanged |

## Slice 2 decisions (2026-07-18)

| # | Decision | Rationale |
|---|---|---|
| S2-1 | HTTP routes keep the legacy paths (`POST /torrent`, `GET /torrents`, …); PLAN's `/add`,`/downloads` are informal labels | SPEC §3.2 freezes the legacy shapes, ARCHITECTURE owns §2.9's endpoints unrenamed, and architecture wins over plan (P2) |
| S2-2 | The `-` stdin add is implemented (io.ReadAll → the shared `AddTorrentBytes` path, persisted `added_via:"file"` with a torrents/ copy); `POST /torrent` stays magnet-only | One code path for file/stdin gives Slice 3 a single signature-verification insertion point |
| S2-3 | `PATCH` and `POST` both serve the merge handlers on `/config/rate-limit` and `/config/queue` | Legacy full-body POSTs behave identically under merge; the DoD's PATCH works; nothing regresses |
| S2-4 | Pause/resume/remove routes land now (legacy controlOne shape) | The engine implements them in this slice; leaving them API-less would strand tested behavior |
| S2-5 | The CLI drops legacy's `DownloadAll()` after GotInfo | The engine's per-file Normal flip is the §5-mandated activation and DownloadAll would bypass the queue cap; scenarios assert all-normal priorities instead |
| S2-6 | `AddTorrentMetaInfo` returns `*engine.Handle`, not legacy's `any` | The `any` existed only so companion could avoid importing engine; the rebuild's daemon-adapter rule makes it unnecessary |
| S2-7 | `--regtest` and `--no-dht-publish` flags deferred (S0-9 dead-flag principle); `--dht-insecure`/`--dht-bootstrap`/`--no-dht`/`--leech-only`/`--no-index` land now with consumers | Regtest timings and Layer-D publishing arrive in Slices 7–10/9 |
| S2-8 | `config.DisablePortForwarding` added (maps to anacrolix `NoDefaultPortForwarding`); the harness sets it | Tests were reaching the real gateway over UPnP — a hermeticity leak; also a legitimate operator knob |
| S2-9 | `fileTracker` builds piece spans from `t.Files()` (`BeginPieceIndex`/`EndPieceIndex`) instead of legacy's info-offset math | Observables identical (Path=DisplayPath, same spans); less duplicated arithmetic |
| S2-10 | Honest lock names replace the legacy `-Locked` inversion (`promoteQueued` acquires its own locks); `queued` state not persisted, re-derived on restore | SPEC §6.3 pins the naming trap; a rewrite following legacy names would deadlock |
| S2-11 | `VerifyData` runs on metainfo-add and restore only (legacy shape) — a plain `.torrent` add with pre-existing data shows 0% until restart | Matches legacy; the restart path covers the common case; noted as a known gap |
| S2-12 | `status` gains a best-effort Downloads section (text + `"downloads"` JSON key); any `/torrents` fetch failure silently omits it | New surface with no legacy anchor; DoD only pins "percentage visible"; older/controller-less daemons keep working |
| S2-13 | CI exclusion moves from end-anchored `/internal/testlab$` to `-Ev '/internal/wirecompat/scenarios(/|$)'` | The legacy grep silently ran future subpackages in CI (§6.3-adjacent footgun) |
| S2-14 | Adversarial-review fixes (behavioral): resume-over-a-full-cap now resets file priorities to none when queuing (the legacy shape let it download outside the cap — empirically confirmed; pinned by test); the magnet→metainfo upgrade re-checks removal and uses an existence-conditional session write so `RemoveTorrent` can't be raced into resurrecting an entry; restore cross-checks the torrents/ copy's infohash against the entry key (zombie-entry guard); `watchCompletion` drops its 24h timeouts (multi-day downloads must still promote) and gains its own regression test; duplicate metainfo adds skip the re-verify rehash; the mkdir-torrents failure is fatal to `engine.New` (legacy shape) while a corrupt manifest degrades self-healingly (session keeps its paths so the next save rewrites it); `Engine.Close` is concurrency-safe (late callers block on the first teardown); pieceSubscription closes its channel on every exit path; the legacy session error-string wrappers are restored | Each was a review finding with a confirmed failure scenario |
| S2-15 | Adversarial-review resolutions (fidelity/quality): `AddTrustedPeerEngine` returns AddClientPeer's real added-count; queue moves stay runtime-only (persistState calls dropped for legacy parity); Ctrl-C during the metadata wait is silent on stdout (legacy contract, pinned); `contracts/bencode` is wired into `AddTorrentBytes` as the strict validator + infohash cross-check (no longer test-only); the DHT `/status` block is wired via `httpapi.Options.DHTStats` (omitted = disabled, present-zero = isolated); `--dht-insecure` stays documented in help WITH its gate noted — resolving the SPEC §6.3 under-documentation defect in favor of discoverability over the legacy's deliberate omission; CI gofmt now covers `contracts/` | Byte-fidelity restored where pinned; contested doc points resolved and recorded |

## Slice 3 decisions (2026-07-18)

| # | Decision | Rationale |
|---|---|---|
| S3-1 | `contracts/sign` owns payload + `Signature` + sentinels + crypto/length checks; `internal/signing` does the .torrent-level field work and returns `(sign.Signature, error)` — ARCHITECTURE's `(pubkey, error)` sketch is satisfied via `Signature.PubKeyHex()` | Only the .torrent layer can detect missing fields (ErrNotSigned); the populated-on-ErrBadSignature Signature must surface, which a bare pubkey return cannot |
| S3-2 | **J1 fixed (deliberate divergence):** `create --sign --seed` feeds the SIGNED bytes to the engine via the new `AddTorrentBytesSeedFrom`, so the creator's own node verifies, persists, and displays its signature | The legacy re-marshaled the unsigned struct and the publisher's own node never badged itself — recorded as legacy defect (a) in the extraction |
| S3-3 | Duplicate adds upgrade `SignedBy` stickily (verified signature fills an empty attribution; an unsigned re-add never blanks it) | Extends §7-Q37 stickiness to the add path; without it a signed re-add of a magnet-added torrent stayed unsigned forever |
| S3-4 | No-seed `create` builds no engine at all (`CreateTorrent*` are standalone functions) | A pure hashing run must not bind sockets or create XDG state; SPEC pins the config shape, not the bind |
| S3-5 | `--piece-kib` gains real validation (power of two ≥ 16 KiB, clear error) | The legacy documented the constraint but silently accepted garbage, producing broken torrents — fail closed instead |
| S3-6 | `SignFile`/`VerifyFile` path helpers dropped; the API is bytes-only | Zero production callers in legacy; the create path signs in memory |
| S3-7 | `"signing: bad private key length %d"` lives only in `contracts/sign.Sign` (raw-key callers); `identity.Signer` guarantees length structurally | The check is unreachable through the Signer path by construction |
| S3-8 | Verify-at-add log events keep legacy names (`engine.torrent_signature_verified`/`_rejected`) with `info_hash` replacing the legacy `path` attr | Stdin adds have no path; `info_hash` matches the package-wide log-key convention |
| S3-9 | Adversarial-review fixes: the sticky upgrade also refreshes the persisted torrent copy (stored bytes always back the claimed `signed_by`) and uses a CAS so concurrent signed adds agree; `--piece-kib` gains an overflow guard (wrap-around bypassed the S3-5 validation — empirically confirmed); empty-directory create fails closed (`root directory contains no files`); `create --no-dht` added so hermetic/local seeding needs no gateway or public bootstrap (the DoD download-from-seeder check rides it); the seed paths dedup into one `addSeedSpec` helper; the anacrolix singleton-list-unwrap leniency in Verify is pinned by test as accepted surface; slog-capture tests freeze the signature event names/attrs; DoD script gained trap cleanup, timeouts, a restart-`signed_by` check, and an end-to-end download from the signed seeder | Each was a review finding; the two majors were coverage gaps (restore of signed_by and AddTorrentBytesSeedFrom serving bytes), both now proven at binary level |

## Slice 4 decisions (2026-07-18)

| # | Decision | Rationale |
|---|---|---|
| S4-1 | **Real bug found + fixed:** `ReadSeekerAt` is size-bounded (`NewReadSeekerAt(rs, size)`). anacrolix `File.NewReader().Read(buf)` reads up to `len(buf)` from storage and only sets EOF *after* the position passes the file end — so a buffer larger than the file over-reads into the following files. Every earlier file in a multi-file torrent was mis-indexed (a `.txt` saw later binary bytes → "NUL detected" skip); the last file coincidentally worked | The shim bounds every Read/ReadAt to the file length; pinned by `TestMultiFileEachIndexedFromOwnBytes` and the DoD's txt+pdf+zim torrent. Also makes ZIM's internal-offset ReadAt safe near the file end |
| S4-2 | `/search` with a nil index answers **200 with an empty local block**, not 503 — a deliberate override of the package's nil⇒503 law | SPEC §2.9: "always runs Layer L when the index is wired"; the web UI depends on the shape. `/index/stats` (whose whole purpose IS the index) keeps 503 |
| S4-3 | `PickLookupToken` = `MostDistinctive(Tokenize(query))` with **capped** Tokenize; lives in `indexer`. dhtindex (Slice 10) will import `contracts/token` directly, NOT `indexer.PickLookupToken` — the layer diagram gives dhtindex no indexer edge | `contracts/token` is a Tier-0 leaf importable from anywhere; a literal `indexer` import from dhtindex would violate the layering |
| S4-4 | The reachable **Forget** is `RemoveTorrent(ih string, forget bool)` on the controller, composed in the daemon adapter (`eng.RemoveTorrent` then `eng.ForgetIndex`); HTTP `DELETE /torrents/{ih}?forget=1`. Files are kept; a nil index makes Forget a no-op that still returns 200 | Keeps index deletion off the hot engine path; Forget must not fail because search is off (F36) |
| S4-5 | The **hourly rescan** is an engine-owned goroutine on `bgCtx` with a package-var interval; it re-submits completed, not-yet-submitted files (pipeline `WasSubmitted` dedup). The legacy had no recovery despite the docs claiming one | Deterministic bounded recovery of dropped file-complete events (SPEC §5.5); no LLM in the loop |
| S4-6 | Daemon tests default to `NoIndex` (Bleve open is ~1 s each); a dedicated `TestIndexerWiring`/`TestNoIndexLeavesSearch503` exercises Layer L. `dod-slice0/1` pass `--no-index` so their `/status` goldens stay `indexed:false` | Keeps the fast suites fast; the indexer's own package tests cover Bleve exhaustively |
| S4-7 | The ZIM test fixture builder is duplicated into a wirecompat test helper rather than exported from `extractors` | Avoids coupling to another package's test scope; the ZIM format is frozen |
| S4-8 | Chunker offset off-by-blank-run fixed (offsets point at the paragraph's first byte); the dead `gz` extension token kept for list fidelity; the stale "~10 KiB"/"then spaces" chunker doc drift corrected | Zero observable surface change (offset never reaches the doc); matches the contract's recommended resolutions |
| S4-9 | Adversarial-review fixes: the rescan cadence is now a per-engine field from `config.IndexRescanInterval` (0 = 1 h) instead of a package var — the var was read by every `engine.New`'s rescan goroutine while a test mutated it, a deterministic `-race` failure; `registerLockedRestore` applies the persisted indexing/signedBy/queueOrder BEFORE spawning the index goroutines (a restore-time race could index an OFF torrent or blank a signed_by — pinned by `TestIndexingOffSurvivesRestart`); `ForgetSubmitted` also clears the pipeline counters (no double-count / unbounded growth on churn); direct-Bleve `search` fails closed with a route hint instead of hanging when a daemon holds the index lock; the CLI restores the legacy per-torrent `tracker:` line and renders `<mark>` snippets in API mode too; the 2100-doc stats test is `-short`-gated; `TestNoIndexStats503ButSearch200` renamed to match what it asserts | Each was a review finding; the rescan race is the notable one — a genuine `-race` regression this slice introduced |

## Slice 5 decisions (2026-07-18)

| # | Decision | Rationale |
|---|---|---|
| S5-1 | **The confirm/flag state transition is ONE deterministic code path in the daemon** (`daemon.Confirm`/`daemon.Flag` → `engine.ConfirmHit`/`engine.FlagHit`). httpapi and the CLI only marshal DTOs and map error sentinels; no LLM, prompt, or per-frontend logic touches Bloom/reputation state | production-architecture-rules.md: exactly-once / state-transition actions live in deterministic code with a fail-closed terminal outcome; the three frontends share the one path |
| S5-2 | Admission is **deny-by-default** (`admission.DefaultPolicy`), inverting the legacy's permissive §6 admission | A spam-resistance boundary must reject the unknown, not admit it; `PermissivePolicyForTest` keeps the old behavior reachable only from tests |
| S5-3 | The Bloom is a **frozen FNV-64a Kirsch-Mitzenmacher v1 `SBLM` format** pinned by a golden byte-vector (`golden_vector_test.go`) and a checked-in `testdata/known-good.bloom` (population 17, k=7) | Format drift silently breaks every existing `known-good.bloom`; the golden makes drift a failing test, and the file fixture proves cross-version load |
| S5-4 | Reputation is **Bayesian-smoothed** (prior weight 5.0, neutral 0.5, seed bonus 0.45·2^(−age/90d)); `Snapshot()` is score-sorted with a deterministic tie-break | A raw good/bad ratio is unstable at low counts; smoothing toward neutral resists both cold-start noise and single-flag swings |
| S5-5 | Two Bloom auto-confirm paths, both **`bloom.Add` only, never `RecordConfirmed`** (D22): trusted-publisher confirm fires at **metadata arrival** (no download needed), completion confirm fires **unconditionally** on `Complete()`. `watchCompletion` **promotes the queued slot BEFORE** the Bloom side-effect + checkpoint | Completion/trust are self-signals that must not self-reinforce reputation; promotion is latency-sensitive and must never wait on the Bloom nil-check or the synchronous checkpoint's disk I/O (a promote-after-checkpoint ordering caused a `-race` `TestCompletionPromotesQueued` timeout) |
| S5-6 | Flag **fails closed on zero attribution** — demotes nobody, never the legacy demote-every-indexer fallback — and reports **honestly**: `indexers_flagged` is NOT `omitempty` (0 renders), `attribution` ∈ {`targeted`,`trusted-exempt`,`trust-unavailable`,`none`}, and the CLI prints "no reputations changed …" whenever nobody was demoted | Fixes the legacy §6 dishonest-success defect (flag claimed success while demoting nobody); trusted publishers are exempt, filtered BEFORE `RecordFlagged` |
| S5-7 | **Review fix (HIGH):** Bloom + reputation `Save` write a **unique `os.CreateTemp` file** (not a fixed `<path>.tmp`), and `engine.Checkpoint` is serialized by `ckptMu` with `Close` **joining the checkpoint goroutine** (WaitGroup) before its final flush | Six unsynchronized goroutines call `Checkpoint`; on a fixed tmp name two concurrent saves of different-length JSON tore each other's writes into an invalid `reputation.json` that rename then published, silently losing ALL confirmed reputation on restart. Pinned by `TestConcurrentTrackerSaveNoCorruption` |
| S5-8 | **Review fix (MED):** a corrupt/unreadable but **configured** `trust.json` fails **CLOSED** — `loadSpamResistance` sets `trustFailed`, and `FlagHit` returns `attribution=trust-unavailable` and demotes nobody rather than treating a nil store as "trust off" | Otherwise a hand-edit typo silently disabled the trusted-publisher exemption and demoted trusted maintainers under a legitimate-looking `Flagged=1`. The attribution is preserved (no `Forget`) so a retry after the file is fixed still acts. Pinned by `TestFlagFailsClosedWhenTrustDegraded` |
| S5-9 | **Review fix (LOW) + polish:** `--help` documents `--api-addr` for `confirm`/`flag`; offline `trust add` `MkdirAll`s its parent dir (clean-install fix found by the DoD); `EstimatedItems` reports a clean `0` instead of negative zero at empty population | Small correctness/discoverability gaps caught by the review and by driving the real binary |

## Slice 6 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S6-1 | The 64-bit services mask has exactly ONE producer: `contracts/ltepwire.Announced(Sharing, RuntimeFacts) uint64`, a pure total function with **no static `DefaultServices()` floor** | Fixes the legacy §7-A5 [weird] defect where the producer OR'd in `0x2ED`, so operator downgrades never changed the advertised bits. Announced derives every bit purely from its inputs; the HTTP readout and (Slice 7) the wire announce call the same `Engine.ServicesMask()` |
| S6-2 | **Type split:** `Sharing{ShareLocal uint8; FileHits, ContentHits bool}` (operator, bits 0–3) vs `RuntimeFacts{Publishing, Reconciliation, Regtest, CompanionPub, CompanionSub, SnippetHighlight bool}` (daemon, bits 4–9). The `PATCH /capabilities` body has **no publisher field** | The legacy §6 "save sharing" clobber (POST replace-whole-struct zeroed the omitted Publisher) becomes **structurally impossible**: the daemon-owned bit is unrepresentable in the setter path |
| S6-3 | `/aggregate` and `/capabilities` `services` render the **live** mask via `httpapi.servicesHex()` (a single `fmt.Sprintf("%016x", ServicesReporter())` path; `handleAggregate` overwrites the field). The daemon adapter no longer sets a static value | Fixes the legacy §6 static-`0x2ED`. httpapi renders the hex itself (uint64 in, no `contracts/*` import) so the zero-subsystem-import law holds |
| S6-4 | `RuntimeFacts.Publishing = !cfg.NoIndex && !cfg.DisableDHTPublish` (config gate, computed live each call) | Matches the legacy gate and makes the `--no-index → Publisher-bit-zero` cascade observable at daemon start (the DoD). Advertises `BitLayerDPublisher` before Layer-D actually pushes (Slice 9) — a deliberate capability-intent bit, to be tightened when Layer-D lands |
| S6-5 | Build-feature bits 5,6,7,9 (Companion pub/sub, SnippetHighlight, SetReconciliation) are set **true** unconditionally in Slice 6 | Keeps the default mask byte-identical to the legacy `DefaultServices()` = `0x2ED`. SnippetHighlight already exists (Slice 4); companion/reconciliation subsystems land later. Deliberate temporary over-advertisement, re-gated per slice as each subsystem arrives; harmless in Slice 6 (no wire consumer yet) |
| S6-6 | **Behavior improvement over legacy:** `RuntimeFacts.Regtest → BitRegtest` (bit 8) iff `cfg.Regtest`. The legacy never set bit 8 despite the "loud" intent | Default `cfg.Regtest=false` keeps the default mask `0x2ED`, so it is a safe strict improvement that finally realizes the loud regtest announce. Pinned by golden vector `0x3ED` |
| S6-7 | `file_hits`/`content_hits` JSON change from legacy `int(0/1)` to `bool`; `share_local` stays `int` (clamped 0..2). `PATCH` is primary + `POST` alias, preserve-unset merge (pointer fields) | Mirrors the `/config/rate-limit` PATCH-merge §6 fix; the API surface is being rebuilt and no in-tree consumer (the minimal embedded web UI has no capabilities panel yet) depends on the integer form |
| S6-8 | Golden vector table in `contracts/ltepwire/services_test.go` pins `Announced` for 6 explicit inputs (incl. the `0x2E0` downgrade regression guard and the `0x2EC` out-of-range-ShareLocal case); the frozen bit constants have their own freeze test; unknown-bits-ignored is a test | The producer is the wire contract — a byte-exact golden makes any drift a failing test, per the contracts-tier convention |

## Slice 7 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S7-1 | The sn_search message envelope (query/result/reject/peer_announce = msg_types 0–3; 4–8 reserved) lives in `contracts/ltepwire/wire.go` with byte-exact golden vectors, encoded via `anacrolix/torrent/bencode`.Marshal directly. `contracts/bencode` stays decode-only | The golden bytes assume anacrolix's sorted-key output; a second impl must match. `services` on the wire is a bencode **integer** (`Announced()` output), not the `FormatHex` string (that is only the HTTP readout) |
| S7-2 | **The transport is token-gated with NO bare-address send API.** `Transport.SendExtension(PeerToken, []byte)` is the only sender; a `PeerToken` has unexported fields + no exported constructor and is minted ONLY inside `OnRemoteHandshake` from a recorded m-dict advertisement — never from a `peer_announce`. anacrolix's `WriteExtendedMessage` fail-closes as a backstop | Makes "cannot send sn_search to a peer that never advertised it" a **compile-time** property — the core of the vanilla-silence guarantee |
| S7-3 | Inbound frames are dispatched **off the anacrolix read loop** (which holds the client lock — a synchronous reply→`WriteExtendedMessage` would self-deadlock): admit via a 256-slot handler semaphore, **copy the payload** (the decoder reuses its buffer), spawn a goroutine with a separately-gated (256-slot) reply writer that copies the body again; a full semaphore DROPS the frame | SPEC §5.4 async-dispatch quirk; a multi-ms Bleve search must never stall every peer under the global lock |
| S7-4 | Layer isolation preserved: `swarmsearch` never imports Bleve (answers via the injected `LocalSearcher`/`LocalHit` mirror) nor the `torrent` package (engine injects `Transport`); `httpapi` never imports `swarmsearch` (the daemon `search_adapter` translates `QueryResponse → SwarmBlock`). A Layer-S error is surfaced **inline** (200 + `swarm.error`), never a 5xx — Layer-L stays fatal-500 (§5.9) | The three-layer strict-isolation law; the CLI/web-UI header rendering depends on the 200-with-error shape |
| S7-5 | **§6 defects fixed:** valid-bencode wrong-shape query charged `ScoreBadBencode` (was free); zero/negative `AddedAt` omits the wire `t` (was the year-1 `-62135596800` stamp); `Hit.N` truncated to 60 bytes rune-safe; outbound `peer_announce.services` = `ltepwire.Announced()` with no static floor (downgrades reach the wire). Result/reject anti-spoof: a frame counts only if its txid matches a live pending AND the sender is in that query's immutable asked set; malformed charged BEFORE the lookup | The four §6 defects the extraction flagged, each pinned by a test; the asked-set anti-spoof is the §5 invariant |
| S7-6 | **Scope-bounded:** the AddrMan **PeerBook, feeler, and HitCache are DEFERRED** (nil-safe local optimizations with zero wire impact; `selectTargets` falls back to "all supported peers"). Everything the DoD and the wire-compat matrix require is present | The ledger sanctioned the deferral (they are nil-safe optionals); it keeps the largest slice bounded without cutting any wire behavior — a Slice-7.1 follow-up |
| S7-7 | **Review fix (MED):** bit 9 `BitSetReconciliation` is **re-gated OFF** (`RuntimeFacts.Reconciliation = false`) until the RIBLT sync bodies land in Slice 8 — the S6-5 "re-gate per slice" promise. Slice 6 advertised it as a harmless build bit (no wire consumer); Slice 7 adds the `peer_announce` wire consumer AND a misbehavior-charging reject for sync frames, so advertising bit 9 would arm a 24h-ban trap for a future Slice-8 peer. Default mask is now `0x0ED` (`0x0FD` publishing), was `0x2ED`/`0x2FD` in Slice 6 | A node must not advertise a capability it answers with a ban; clearing the bit makes the sync-frame gate valid (a well-behaved peer never sends them) |
| S7-8 | **Review fix (MED):** the pending-query `asked` set is now **consumed per-peer** — each asked peer contributes at most one frame (`firstFrom`), so a single peer cannot flood the results channel, end collection early, inflate `Responded`, or monopolize the merged set. The pending entry is retained for a **10 s grace** after collection so an honest asked peer replying just after the deadline is matched-and-dropped, not charged `ScoreStaleTxID` | Closes a one-peer search-integrity DoS + a false-ban vector against slow honest peers |
| S7-9 | **Review fix (LOW):** `truncateUTF8` walks runes from the FRONT instead of back-stripping, so a non-UTF-8 torrent name (common for older/Asian torrents) longer than 60 bytes degrades to a byte cap rather than collapsing to the empty string | A back-strip empties any name whose invalid byte is near the head; the front-walk keeps the name |

## Slice 8 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S8-1 | `contracts/riblt` freezes the RIBLT math: `Key()` = FNV-1a-64 over ALL 32 bytes (a linear prefix key would let the decoder hallucinate pure symbols), `contributes(key,idx)` = SplitMix64(key+idx·golden) mod 2^(1+idx%12), the streaming Encoder, and the peel Decoder (pure-symbol detect + a `Key()==KeyXOR` self-consistency gate). Golden-vector pinned; stdlib-only, no crypto | The coded-symbol bytes are a cross-impl wire contract; a homegrown minimal RIBLT (Yang et al. 2024), NOT the yangl1996 library |
| S8-2 | `contracts/record`: `ElementID = SHA-256(pk‖kw‖ih‖LE64(T))` **EXCLUDES** pow+sig (so two valid signings of one semantic record dedupe to one RIBLT element); `SigMessage` = the ElementID preimage **‖ uvarint(Pow)** (pow is inside the signature, un-strippable); `SignAndMine` mines THEN signs. Golden-pinned | The single source of truth for the record id + signature; kills the legacy's three-way `localRecordID`/`cacheRecordID` byte duplication (`swarmsearch.LocalRecord = record.Record` alias) |
| S8-3 | `contracts/ltepwire` gains the sync codec (msg 4–8): `sync_begin/symbols/need/records/end` with caps (symbols ≤100, records ≤500, ids ≤1000), `element_size == 32`, `kw ≤ 64` enforced at decode. Golden-pinned. `services` `h`/KeyXOR is a bencode uint64 that may exceed 2^63 | Frozen wire; the same anacrolix-bencode sorted-key encoding as msg 0–3 |
| S8-4 | `SyncSession` (I/O-free): budgets negotiate **downward only** (min of peer + own, 2000 symbols / 1 MiB); byte accounting on the **semantic** record size `32+len(kw)+20+8+8+64` (never bencoded); a symbol-budget overrun → `limit_exceeded` (**penalty-free** — SPEC over legacy, which charged); every OTHER violation → `aborted` + charge. `ApplyRecords` rejects records only in `Idle`/`Begun` (relaxed from "Needed/Fulfilled only" because the rebuild dispatches inbound frames on independent goroutines) | The frozen §5.1/[weird] budget rules, adapted to the async dispatch |
| S8-5 | Multi-batch: the responder pumps `sync_symbols` batches until the initiator sends `sync_need` (converged) or the budget is hit (final `done=1`); the initiator fetches `Added` via `sync_need` and **proactively pushes** `Removed` records, giving bidirectional symmetric-difference convergence (>100-symbol diffs converge). The legacy responder streamed one batch only | The Slice-8 charter exceeds the one-batch legacy responder |
| S8-6 | The engine mints one signed record per torrent name-keyword on `GotInfo` (`contracts/token.TokenizeAll`), gated on the signer+cache and placed **before** the Layer-L gate so a `--no-index` node still contributes; `RecordCache` (FIFO cap 100k, age-prune 30d) is both the sync source and sink; `Engine.SetSigner` is called by the **daemon** (which owns identity) before `RestoreSession`. Bit 9 re-enabled (`RuntimeFacts.Reconciliation = true`; masks back to `0x2ED`/`0x2FD`) — the S7-7 re-gate promise fulfilled. `/aggregate` gains `cache_size` + `reconciliation` | Records ride the reconciliation wire (Layer-D DHT publish is Slice 9); PoW bits = 0 this slice (a dual-read migration window precedes raising it) |
| S8-7 | **Review fix (HIGH):** `ApplySymbols` is **reorder-tolerant** — an out-of-order batch is buffered by stream index, a stale/duplicate batch is dropped idempotently, the contiguous prefix is applied, and a gap larger than the 32-batch reorder buffer aborts | Slice 7's goroutine-per-inbound-frame dispatch reorders a session's symbol batches; the strict `index==symbolsIn` check would falsely hard-abort every multi-batch sync over the real engine transport |
| S8-8 | **Review fix (HIGH+MED):** the initiator finalizes only when the decoder converged AND `symbolsIn ≥ 256` (the floor), OR a `done=1` batch arrived — and fires **exactly once** (a `convergeFired` guard). `WaitSyncConverged` waits on this finalize, not on bare `Converged()` | `Converged()` ("all residual symbols zero") is trivially true on a too-short prefix, silently losing an element that first contributes past the prefix; and without a once-guard, post-convergence in-flight batches re-fire `initiatorConverge`, duplicating needs/pushes and tripping a spurious byte-budget `limit_exceeded` |
| S8-9 | **Review fix (MED+LOW):** the per-peer session cap is enforced **atomically** (check+register under one lock — was TOCTOU-racy); `sync_begin` is throttled on the shared per-peer token bucket (the expensive session-opening verb); `RecordCache.order` is compacted on age-prune so it does not grow unbounded | Closes a concurrent-`sync_begin` cap bypass, an unthrottled-session-open vector, and an order-slice memory leak under long-lived pruning |

## Slice 9 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S9-1 | `contracts/dhtschema` freezes the Layer-D wire: `KeywordValue{ts, hits[], more, next_pk}` / `KeywordHit{ih, n, s, f, sz}` bencoded via **anacrolix `torrent/bencode`** (NOT `contracts/bencode` — the put path re-decodes and hands to `bep44.Put.Sign`, which must re-marshal byte-identically). `EncodeValue` stamps a live `Ts`; `DecodeValue` rejects `>1000` bytes **before** `Unmarshal` (inclusive cap — exactly 1000 decodes); `SaltForKeyword` is the keyword bytes **verbatim** (no lowercasing, no truncation, ≤64 bytes) so the SHA1(pk‖salt) target stays cross-impl identical; `more`/`next_pk` ship on the wire but are never populated in v1. Golden-pinned + a bep44 re-marshal-identity gate + a legacy-bytes read-back gate | The BEP-44 `v` field is a frozen cross-impl contract a vanilla client verifies; kept SEPARATE from `contracts/record` (the RIBLT substrate never rides the BEP-44 wire) |
| S9-2 | The **`RecordBackend`** swappable seam (`Publish/Refresh/Retract/Lookup/Status/Close`): `legacyKeyword` is the v1 default, selected by `config.LayerDMode == "legacy"` via a one-line `NewBackend` switch. `Putter`/`Getter` + the fail-closed `checkPutStats` live **beneath** the port (shared by every backend so the guard cannot drift); the manifest/throttle/eviction are format-specific state **inside** the backend; the indexer-set/reputation/scoring orchestration sits **above** the port in `Lookup`. Record bytes stay codec-owned in `contracts/dhtschema` (never exposed through the port) | Slice-12 adds `aggregatePPMI`/`composite` behind the same port with no change to `Publisher`/`Lookup`/`searchmux`/`httpapi`; the port does not become a lowest-common-denominator between the legacy CAS and the B-tree/RIBLT models |
| S9-3 | `internal/dhtindex.Publisher` is a thin worker (buffered `Submit` + hourly `Refresh` ticker + `Retract`) driving the backend; `legacyKeyword` owns the persistent `Manifest` (`publisher.json`), the **55-minute per-keyword throttle** (skip the put with no network I/O — anacrolix has no put rate cap), **oldest-hit eviction** keeping the live encode ≤1000 B, and `MarkPublished`/`MarkFailed`. `checkPutStats` **fails closed** on a zero-node put (`getput.Put` returns a nil error even at zero nodes → `MarkFailed`, not `LastPublished`, so the throttle never suppresses an undiscoverable keyword's retry). `Exp=2h` stays **engine-owned** (`ConfigureAnacrolixDhtServer`) and is NOT re-added here | The self-DoS guard + fail-closed semantics are load-bearing; leaving `Exp=0` makes every stored item instantly expired ("put succeeds, get finds nothing") |
| S9-4 | `Lookup` derives the keyword from **`token.MostDistinctive(token.Tokenize(query))`** (the §6 fix: "new ubuntu" → the `ubuntu` target, never the first token "new"); imports `contracts/token` directly (no `internal/indexer` edge). Parallel per-indexer fan-out (one responder suffices), merge by 40-hex infohash (max seeders / first non-empty name / accumulate sources), score = mean-rep (0.5 w/o tracker) + 0.05·extra-source (cap +0.2) + 0.25 Bloom, sorted `BloomHit, Score, sources, Name`. Optional reputation cutoff (`MinIndexerScore`, default 0 = off). PPMI fields **omitted** this slice; multi-word stays single-keyword DHT + client-side intersection (AND/OR NEVER pushed to the DHT) | The most-distinctive chooser is the single cross-impl DHT-target rule; the port (not response fields) is the Slice-12 Aggregate forward-compat seam |
| S9-5 | Engine wiring: the **read side** (`Lookup`) is built in `New` when the DHT is on and stays alive even with no identity (**leech-only Layer D**); the **write side** (`Publisher`) is built lazily in `SetSigner`, only when an identity is present **and** publishing is active. Publish-on-`GotInfo` submits `token.Tokenize(name)` — **NAME keywords only, never content tokens** — alongside record-minting, **before** the Layer-L gate (per-torrent-indexing-off still publishes existence). `--no-index` / `--no-dht-publish` suppress the write side; `RemoveTorrent` retracts; the self-pubkey auto-enters the lookup set; `peer_announce` gossip enters it via `NotePublisherSeen` | The privacy cascade must suppress network-visible publication under the user's key while keeping read/pointer paths alive; content tokens on the DHT would leak file contents (the §5.2 [weird] invariant) |
| S9-6 | `searchmux` gains the third concurrent layer (`DHTSearcher`); the three native response types (`indexer`/`swarmsearch`/`dhtindex`) are **never merged** into a shared hit type. §5.9 asymmetry: a Layer-L error is fatal (500), a Layer-D error is surfaced **inline** (200 with `dht.error`). `httpapi` keeps the **zero-import law** (new `DHTBlock`/`DHTHit` DTOs; the daemon `dhtBlock`/`publisherStatus` adapters translate field-by-field); a dedicated `GET /publish` route reuses the frozen `PublisherStatus` DTO and folds totals into `/status`. The `/search` context deadline is the max of the swarm/DHT budgets + 500 ms grace (DHT default 5 s, ≤30 s) | One fan-out shared by every frontend keeps reconciliation from drifting; the isolation boundaries and the error asymmetry are ARCHITECTURE law |
| S9-7 | New config: `LayerDMode` (`Validate` accepts only `"legacy"` this release), `MinIndexerScore` (default 0), `PublisherPath` (`publisher.json`, empty = in-memory). New `SampleInfohashes` primitive (standard **BEP-51** verb, nil-guarded) + the stateless `crawl-probe` CLI (throwaway `127.0.0.1:0` `Passive`+`NoSecurity` server, fresh-random target default, `--json`). The **BEP-46** infohash-pointer primitive (`PutInfohashPointer`/`GetInfohashPointer`, sharing `checkPutStats` + the ≤1000 pre-unmarshal decode cap) lands with the package for Slice-10 companion consumption. `--no-dht-publish` flag added to `add`; `--help` lists `crawl-probe` + `--no-dht-publish` + a runnable `search --dht` example | Layer D rides ONLY BEP-44/46/51 — no new verb, reserved bit, port, or classifiable marker; the crawler worker-pool + PPMI/Aggregate are deferred to Slice 12 |
| S9-8 | **Review fix (MED):** `Lookup`'s merge dedups per indexer — a single indexer contributes each infohash to the source count **at most once** (a `seenFromIndexer` set per fetch result). Without it, one hostile indexer repeating an infohash in its own `KeywordValue.Hits` faked multi-indexer consensus: inflating the `+0.05/extra-source` bonus (up to +0.2) and the source-count sort tiebreak, so a single-indexer spoof could outrank a genuine 3-distinct-indexer hit | The +per-source bonus and source-count sort ARE the Layer-D anti-spam signal (distinct-indexer consensus); the read path applies no dedup (`DecodeValue`/`backend.Lookup` return hits verbatim), so a crafted value defeated it. `SourceTracker.RecordMany` already dedups into a set — this mirrors that guarantee in the scoring path |

## Slice 10 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S10-1 | `internal/companion` is a new package that talks to the DHT and engine ONLY through narrow port interfaces (`PointerPutter`/`PointerGetter` = the Slice-9 BEP-46 primitives, `TorrentSeeder`, `CompanionFetcher`) — it never imports `internal/dhtindex` or `internal/engine`. It imports `internal/indexer` for the corpus doc types (`CorpusSource`/`Ingester`), an acyclic search-layer edge, not a DHT/engine edge | The §5 "narrow ports only" rule; keeps the companion subsystem swappable and testable with fakes, and the wire primitives single-sourced in Slice 9 |
| S10-2 | The `CompanionIndex` is a gzip-JSON document (`{version, format, publisher, generated_at, torrents[]}`), SEPARATE from the SNAGG B-tree / PPMI / hashcash-PoW path (`build_btree.go`/`pow.go`), which stays deferred to **Slice 12**. `Encode` force-sets format/version + normalises nil torrents to `[]`; `Decode` bounds decompression at 1 GiB and refuses an unknown format/version before trusting records | The simple path is the whole of Slice 10; the B-tree/PPMI is the Aggregate index (Slice 12) reached through a different builder the daemon never calls |
| S10-3 | The publisher rebuilds+republishes on a ≤1h ticker (the BEP-46 pointer TTL is 2h): build from the local index → **an EMPTY index is a FAILURE** (`"nothing to publish"`) → write the gz-JSON + a single-file 256-KiB-piece trackerless `.torrent` (atomic tmp+rename) → seed it IN PLACE from the companion dir (`AddTorrentMetaInfoSeedFrom`, since the companion dir ≠ the data dir) → `PutInfohashPointer` at salt `"_sn_content_index"`. `lastRefresh` advances **only** on a fully-successful publish; the `RefreshNow` throttle is measured on `lastAttempt` so a failed retry cannot masquerade as a recent success. `checkPutStats` fail-closed means a zero-node pointer put is a failure | The §5 state-advance rules (empty=failure, lastRefresh only on success) + the fail-closed put; seeding in place is required because the payload lives in the companion dir, not DataDir |
| S10-4 | The subscriber is the security-critical path and FIXES all three legacy §6 defects: (a) after decode it **rejects a snapshot whose `Publisher` != the followed pubkey** before any ingest; (b) it **stamps `SignedBy` = the followed pubkey** on every imported `TorrentDoc` (so `search --signed-by` attributes them); (c) it **dedups on `GeneratedAt`** (a `Subscriber.imported` map) so an unchanged snapshot is not re-ingested every interval. The fetch is FAIL-CLOSED in the ENGINE (`FetchCompanionTorrent` → `validateCompanionInfo`): exactly 1 file, ≤32 MiB checked BEFORE any piece, and a safe filename (reject `""`/`.`/`..`/`/`/`\`) — only then is `filepath.Join(DataDir, info.Name)` returned | The BEP-46 pointer resolves to a fully attacker-controlled infohash; every bound exists to stop a hostile publisher from path-traversal, gzip-bomb, or identity-spoof of imported search results |
| S10-5 | Daemon wiring: the publisher and subscriber are **independent legs**, each gated (both need the index + companion dir; the publisher additionally needs a pointer putter [DHT + identity] and the identity for its namespace/`Publisher` field), and **every companion setup failure is a non-fatal warning**. Teardown stops the workers BEFORE the index they write and the engine they use (subscriber → publisher → index → engine). The follow-set persists to `companion-follows.json` (a JSON array, atomic tmp+rename, ≤1 MiB fail-closed load, one-bad-row-skipped) | A companion failure must never take down local + Layer-D search; the teardown order prevents a worker writing to a closed Bleve |
| S10-6 | httpapi keeps the zero-import law: a locally-declared `CompanionController` + `Companion*` DTOs, with the daemon `companionAdapter` translating field-by-field. `GET /companion`, `POST /companion/{refresh,follow,unfollow}` (503 unconfigured, 429 throttle carrying `ErrTooSoon`, 400 bad pubkey). The legacy had **no companion CLI** (HTTP + follow-file only — a discoverability gap); the rebuild ADDS `swartznet companion <status\|follow\|unfollow\|refresh>` over the API (pubkey accepted in either flag position), plus a gated `add --regtest` flag for accelerated timings, both indexed in `--help` | Closes the legacy CLI-discoverability gap; the CLI is the surface the DoD drives against the real binary |
| S10-7 | **Review fixes (HIGH + 3×MED):** companion seed/fetch torrents are now a distinct **`companion` engine handle** (`registerLockedCompanion`, `indexing=false`) that `autoIndex` **early-returns on** — so a node's own companion torrents can no longer (a) pollute its published corpus (`BuildFromIndex` would otherwise list its own bookkeeping torrents as content) nor (b) leak `"swartznet-content-index-*"` filenames onto the public BEP-44 keyword index. Companion torrents accumulate no more: the **publisher drops the previous seed** when it publishes a new-infohash snapshot, and `FetchCompanionTorrent` **drops the fetched torrent on every exit path** (data stays on disk to read). The subscriber **dedups on the pointer infohash BEFORE fetching**, so an unchanged pointer needs no re-fetch. Companion seeds are not persisted to the session | The 4-lens review (12 agents) confirmed a self-referential index/Layer-D feedback loop (HIGH) and three engine/goroutine leak paths (MED); each is a correctness/data-integrity bug the fixes close — verified against the real binary (search proves the companion seed is absent from the local index) |

## Slice 11 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S11-1 | `internal/gui` is the native Fyne frontend (fyne.io/fyne/v2 v2.7.3, the FIRST GUI binary), built as **PURE PRESENTATION** over the same `internal/daemon.Daemon` the CLI + web UI use. Five tabs (Downloads, Search, Status, Companion, Settings) each read Daemon/engine accessors and drive actions through the Daemon; the GUI holds NO independent lifecycle, reconciliation, or spam-signal logic. Fyne builds here (CGo + OpenGL/X11 present) and tests headlessly via the `fyne.io/fyne/v2/test` driver | SPEC §2.10 / §5.8: frontends are pure presentation over one Daemon constructor |
| S11-2 | The search fan-out is unified: the daemon now builds the `searchmux.Mux` **unconditionally** (not gated on the HTTP API) as `d.mux` and exposes **`Daemon.Search(ctx, searchmux.Query) searchmux.Result`**. Both the HTTP API and the GUI call this ONE fan-out — the GUI's Search tab renders the three native response types as separate **L/S/D cards** and never merges them (the legacy §5 defect was a GUI-private three-layer reconciliation + its own confirm/flag logic, both deleted). Confirm/Flag on a hit card route through the shared `d.Confirm`/`d.Flag` | The single fan-out keeps every frontend's reconciliation from drifting; deletes the GUI's shadow reconciliation/spam path |
| S11-3 | **§6 fix:** the About dialog states **`Apache-2.0 (SwartzNet) · engine anacrolix/torrent: MPL-2.0`** — the legacy claimed `MIT (SwartzNet code)`, which is wrong (first-party code is Apache-2.0). `Version`/`BuildDate` come from ONE build-stamped source (`cmd/swartznet-gui/main.go` package vars, set by `build-gui.sh` `-ldflags`); the window title + About read only those. The dark theme mirrors the web UI's CSS variables verbatim | CLAUDE.md: new first-party files are Apache-2.0; a single build-stamped version/license source |
| S11-4 | GUI concurrency: each tab owns a poll goroutine (Downloads 1s, Status 2s, Companion 3s) that mutates widgets ONLY through `fyne.Do`; all poll loops stop when the GUI-local context is cancelled. `App.onClose` cancels that context, closes the Daemon exactly once (the GUI owns the single lifecycle), and quits Fyne. The search runs on its own 10s-deadline goroutine and marshals results back via `fyne.Do` | Fyne's widget tree is single-threaded; the poll/search goroutines must serialize mutations through fyne.Do and stop cleanly on close |

## Slice 12 decisions (2026-07-19, in progress)

| # | Decision | Rationale |
|---|---|---|
| S12-1 | `contracts/snagg` freezes the signed SNAGG B-tree byte format: the 6-byte magic `SNAGG\x00`, the 16-byte little-endian page header, the bencoded record (canonical key order `ih<kw<pk<pow<sig<t`), the leaf/interior/root pages with the **MIN-KEY separator** scheme (separator = child subtree's min RecordKey; first child forced −∞), and the **162-byte signed trailer**. The record TYPE + sign/PoW preimage are **reused from `contracts/record`** (Slice 8) — identity (`ElementID`, excl. pow+sig) and the SNAGG fingerprint (`SHA256(concat EncodeRecord)` over the FULL record incl. pow+sig) stay deliberately DISTINCT preimages. `BuildBTree` is deterministic given a pinned `created_ts`/`seq`/`piece_size`; `OpenBTree` verifies the trailer signature before trusting anything; `Find` is an authenticated prefix query (drops bad-sig/PoW records) with hostile-tree guards (shared visited-set defeats DAG fan-in; strictly-increasing-downward child indices); `VerifyFingerprint` is the optional completeness pass. Golden-pinned | The cross-implementation anchor a second implementation must reproduce byte-for-byte; the fingerprint is the PPMI commit |
| S12-2 | `contracts/dhtschema.PPMIValue` freezes the Aggregate pointer: `PPMISalt = SHA256("snet.index")` (the "double-hash" — every publisher shares the salt so a passive DHT observer can't fingerprint SwartzNet by salt; the target is `SHA1(pubkey‖PPMISalt)`), and `PPMIValue{ih, commit, topics, ts, next_pk}` bencoded alphabetically with `commit == the SNAGG tree fingerprint` so a reader verifies the pointed-to tree before trusting its records. Shares the ≤1000-byte pre-unmarshal cap. Golden-pinned (salt `89fe1aae…965d`, 45-byte minimal item) | The frozen addressing + pointer contract; PoW lives on the leaf record, NOT the PPMI (two independent contracts) |
| S12-3 | The offline **`aggregate build\|inspect\|find`** CLI is a thin driver over `contracts/snagg` + `contracts/record` + `identity`, fully offline (no daemon/DHT). `build` signs each JSONL record (auto-creating only the default XDG identity; explicit `--key` is fail-closed), optionally mines hashcash (`--pow-bits`, **refused above 40** with exit 2), packs a signed SNAGG file (**mode 0644**), prints records/pages/bytes/fingerprint; `inspect` is the fail-fast integrity gate (rejects bad magic/version/trailer-sig); `find --verify` re-derives the whole-file fingerprint. Indexed in `--help` | The offline tooling proves the format end-to-end against the real binary; the frozen `--pow-bits ≤ 40` cap + 0644 mode are DoD invariants |
| S12-4 | The **live seam** is wired: `aggregatePPMI` (accumulates signed records → packs an in-memory signed SNAGG tree → serves an authenticated self-lookup via `Find`) and `composite` (DUAL-WRITES legacy-primary + aggregate-secondary; DUAL-READS legacy-then-aggregate, merged + deduped; the primary's error is authoritative, the secondary's is logged-never-fatal). `NewBackend` gains a signer param and the `composite`/`aggregatePPMI` cases; `config.Validate` now accepts `{legacy, composite, aggregatePPMI}` but **the ship default stays `LayerDMode=legacy`**. DoD proven: a composite dual-write followed by a **legacy-only read still returns the hit** (a mode switch is adapter-selection with zero data loss); a broken secondary never breaks the primary. Cross-publisher aggregate discovery (resolving a remote PPMI + fetching its tree) and the read-only aggregate are intentionally **inert** — the DHT distribution is deferred with the crawler | The seam's whole point is that migration is a one-line mode change that can never lose data (legacy always written) nor be broken by an experimental backend (secondary fail-safe) |
| S12-5 | **Remaining (opt-in, off by default, non-load-bearing):** the DHT PPMI publish/resolve distribution (so `aggregatePPMI` discovers OTHER publishers' trees, not just its own), the admission `seeds.json` + `MarkSeeded` bootstrap (deny-by-default anchors), and the BEP-51 crawler worker-pool (`CrawlOnce`/frontier/politeness on top of the already-shipped `SampleInfohashes` primitive). Ship default stays `legacy`; these ride behind the same seam with no app change | The genuinely-new §7-underspecified distribution/crawl work stays non-load-bearing so GA never depends on unproven Aggregate DHT convergence |
| S12-6 | The cross-publisher distribution (S12-5's first item) is now wired via **two narrow ports** on `aggregatePPMI`: `TreePublisher.PublishTree(name, bytes, commit)` and `TreeResolver.ResolveTree(pubkey) → *snagg.Tree`. `Refresh` rebuilds the tree, snapshots `(tree, fp, publisher)` under `mu`, then calls `PublishTree` **off-lock** — a slow seed/DHT put can never stall a concurrent `Publish`/`Lookup`, and the snapshot is race-safe because `rebuildLocked` assigns a **new** `a.tree` slice (never mutates in place). `Lookup` for a non-self indexer routes through the resolver; the local self-lookup path is unchanged. `SetDistribution` overrides only its non-nil arguments, so the read and write sides each wire their own half onto their own backend instance | The port pair keeps `aggregatePPMI` free of engine/DHT/companion imports (the isolation the seam exists for); off-lock distribution keeps the hot query path non-blocking |
| S12-7 | **Resolve failures degrade to no-hits, never a query error.** A cross-publisher `Lookup` returns `(nil, nil)` when no resolver is wired, the publisher is unknown, or the transport fails (logged at debug) — mirroring the legacy backend, where one unreachable indexer never fails the whole fan-out. The **pointer→tree commit binding survives distribution**: `OpenVerifiedTree` rejects a fetched tree whose fingerprint ≠ the PPMI `commit`, and a non-{0,32}-byte commit is refused by `DecodePPMI` before the resolver ever sees it, so a malicious pointer can't make a reader trust bytes the signed pointer didn't vouch for | Availability of one publisher's tree is best-effort; correctness of what a resolved tree contains is not — the binding is the security boundary |
| S12-8 | Live engine adapters (`internal/engine/aggregate_dist.go`) compose **already-tested primitives** — `WrapSnaggTorrent`/`OpenVerifiedTree` + `SeedMetaInfo`/`FetchCompanionTorrent` + `PutPPMI`/`GetPPMI` — behind narrow `treeSeeder`/`treeFetcher`/`ppmiPutter`/`ppmiGetter` interfaces (all satisfied by `*Engine` + the anacrolix put/get), so the glue is unit-testable with fakes and a real on-disk SNAGG round-trip. The publisher writes the tree into `CompanionDir`, seeds it, puts the pointer (`ih == seeded infohash`, `commit == fingerprint`), and **drops the previous snapshot's seed** each refresh. `layerd.go` wires the resolver on the **read** side (getter-only) and the publisher on the **write** side (putter-only) via a `DistributableBackend` type-assert legacy backends don't satisfy — inert unless `LayerDMode ∈ {aggregatePPMI, composite}`. The timing-sensitive **2-engine-over-real-DHT e2e stays deferred to `wirecompat/scenarios`** (local-only, like the companion transfer); the in-CI proof is the backend-level distribute→resolve e2e + the adapter unit tests | Reuse the proven primitives; keep the one flaky integration test out of the merge gate; ship default stays inert |
| S12-9 | **(review fix, HIGH)** The read-side aggregate backend is constructed with a **zero** self-pubkey (`put == nil` ⇒ `selfPub() == [32]byte{}`), so `aggregatePPMI.Lookup` cannot recognize a self-query and would route it through the resolver → `FetchCompanionTorrent(ownIH)`, whose deferred `DropCompanionTorrent` **tears down the node's own aggregate seed** on essentially every local search (both `aggregatePPMI` and `composite` modes) and whose hardcoded `DataDir` return path (the self-seed lives under `CompanionDir`) returns **zero** self-hits. Fix: `aggTreeResolver` carries a lazy `self func() [32]byte` (reads the engine's identity at query time) and **short-circuits a self-lookup to `(nil, nil)` before any fetch** — the node's own hits already come from Layer L, and legacy self-lookup is likewise a redundant network round-trip, so nothing is lost | The remote-fetch primitive is add-then-drop against an UNTRUSTED infohash read from `DataDir`; pointing it at the node's OWN persistently-seeded infohash in `CompanionDir` is both destructive and wrong-path — self must never be resolved over the network |
| S12-10 | **(review fix, LOW)** When the last record is retracted the tree goes empty and `rebuildLocked` sets `a.tree = nil`, so the old `Refresh` returned before `PublishTree` — leaving the previous companion seed serving stale bytes and the PPMI pointer stale until its 2h TTL. Fix: `TreePublisher` gains `RetractTree(ctx)`; `Refresh` calls it when `tree == nil && publisher != nil`, and `aggTreePublisher.RetractTree` drops the held `lastSeededIH` (idempotent). The BEP-44 pointer is still left to expire on TTL (BEP-44 has no delete), matching the companion publisher's empty-is-not-republished behavior | A retracted-to-empty publisher must stop seeding a stale index, not silently keep it alive until engine close |

## Slice 13 decisions (2026-07-19)

| # | Decision | Rationale |
|---|---|---|
| S13-1 | The CI merge gate (`.github/workflows/test.yml`, on PR to `main`) is confirmed complete: it installs the Fyne C deps and runs `gofmt -s`, `go vet ./...`, a `go mod tidy` drift check, and `go test -race` across **every non-`scenarios` package** — which includes all eight `contracts/*` frozen golden-vector suites, the capability-mask future-bit tolerance, and the sn_search reject-code-2 assertion. The timing-sensitive `internal/wirecompat/scenarios` (real-loopback multi-client transfers) stay **local-only** (run via `go test -race ./internal/wirecompat/...`) so the gate never reintroduces flakiness. `dod-slice13.sh` mirrors the gate locally | SPEC §2.11/§5.11: the deterministic half is the merge gate; the timing half stays local |
| S13-2 | **Hardening caught + fixed real CI-gate breakers introduced in Slices 10–12:** four `gofmt -s` violations and a `go vet` "using resp before checking for errors" in a companion test — all fixed, so the gate is green. This is the payoff of promoting `gofmt`/`vet`/`tidy` to a checked gate: local `go test` alone had let them through | A merge gate is only real if it is green; drift accumulates silently without it |
| S13-3 | **Doc reconciliation (§6 doc-drift fixes):** `docs/07` no longer mandates DHT sharding — the shipping v1 does **oldest-hit eviction** to stay under the 1000-byte BEP-44 cap, with the `#<n>` shard scheme + `more` field documented as **reserved-not-wired** scaffolding (matching `dhtindex`/`dhtschema`). Verified still-accurate: `docs/06` reject-code-2 (`RejectUnsupportedScope = 2`), the per-peer token bucket, `int(bleveScore*1000)` rank, and `THIRD_PARTY_LICENSES` listing `ledongthuc/pdf` as **BSD-3-Clause** (not MIT). Crash-safety (≤ one checkpoint interval of Bloom/reputation loss) was already pinned in Slice 5 | SPEC §5.12: docs are normative and must match code; the sharding claim was the one live drift |
| S13-4 | **The deterministic vanilla-silence gate is now in CI** (`internal/swarmsearch/vanilla_silence_test.go`): a `recordTransport` double proves that a peer which never advertised sn_search receives ZERO frames (no peer_announce, no query) while a capable peer gets both — because a `PeerToken` is minted ONLY from an m-dict advertisement and every `SendExtension` requires a valid token, so a vanilla (or banned) peer is structurally unreachable. This runs in the CI `-race` gate (the timing-sensitive real-loopback version stays in `wirecompat/scenarios`, local-only) | The mainline-compat guarantee is the whole project's load-bearing constraint; it now blocks merge deterministically, not just locally |
| S13-5 | **Deferred (documented, not blocking):** a rebuilt `cmd/dht-smoke` with the non-zero-on-all-fail exit-code fix (the tool was never re-created in the rebuild; `crawl-probe` already covers BEP-51 probing) and the netem `testbed` timeout tightening; plus, from Slice 12, the live DHT PPMI distribution + BEP-51 crawler worker-pool | New ops tooling / non-load-bearing distributed mechanisms; the load-bearing gate + docs + vanilla-silence are done |
| S13-6 | **(S13-5 resolved)** `cmd/dht-smoke` is rebuilt with the §6 exit-code fix isolated in `stressFailedHard(sum) = sum.total>0 && sum.ok==0`; `run` returns exit 1 when it holds. Also resolved from Slice 12: the live DHT distribution (S12-6…S12-10) and the crawler worker-pool + `swartznet crawl` CLI. **Remaining ops tail:** ONLY the netem `testbed` timeout tightening (an integration-harness knob, not product code) | The load-bearing rebuild + every documented §6 fix are done; the last item is a test-harness timeout |

## Task 1 (web client) decisions (2026-07-19)

| # | Decision | Why |
|---|----------|-----|
| T1-1 | The web client is **vanilla JS, native ES modules, no build step / framework / CDN**. `index.html` shell + `static/{app,api,util}.js` + `static/pages/*.js` + one `style.css`, served by the daemon through the FROZEN `//go:embed index.html static/*` contract (verified recursive into `static/pages/` by a Go embed-manifest test). The stub `index.html`'s `<link>` stays; a `<script type="module">` entry + `<header>`/`<main id="view">` are added | A Go repo should not grow a Node build toolchain; `go:embed` needs the assets self-contained; ES modules load over http with zero tooling |
| T1-2 | **Three-frontends-one-daemon: the web client is PURE PRESENTATION.** It owns zero node state, issues one request per action + one poll per visible tab, and never implements retries/queues/state machines (Production Architecture Rules — deterministic control stays in the daemon). `api.js` is the single seam and encodes the two contract invariants: **non-2xx = plain-text body → `ApiError`** (never `JSON.parse` an error; the only structured errors are the inline `swarm.error`/`dht.error` inside a 200), and **writes are same-origin JSON** so `withCSRFGuard` passes with no auth token | One place owns the fetch rules; the CSRF-guard + loopback-bind IS the security model, so the client must never add an Origin override or run off-loopback |
| T1-3 | **The L/S/D search layers render as three strictly-separate card groups — never merged, cross-sorted, or deduped** (same isolation as `gui/search.go`); a swarm/dht group renders only when it was requested AND present; an inline `swarm.error`/`dht.error` (HTTP 200) becomes that group's status line, not a thrown error. Each block's score is formatted by its real wire type: **local/dht float, swarm int**. Highlight fragments are escaped then only `<mark>` is re-allowed (no other markup survives → no XSS) | The three-layer strict-isolation architecture anchor is load-bearing; a unified result list would reintroduce the reconciliation the rebuild deleted |
| T1-4 | **Verified by a live smoke test, not just unit checks.** `scripts/smoke-web.sh` boots a real throwaway daemon (`add --no-dht --no-index <bare-ih>`, flags-before-positional) and asserts 21 things: SPA served + `/static` js + 404s, every read endpoint (200, or 200/503 for the opt-in ones), the CSRF guard (loopback POST 200 vs cross-origin 403), a PATCH merge (`upload` set, `download` untouched) + the `share_local` 9→2 clamp, and the plain-text 400 error shape. `dod-webclient.sh` wraps the embed test + `node --check` + the smoke. The magnet-only Add, the real per-file-priority UI, and the HTTP-only About are documented divergences from the GUI | A web client's real contract is the running daemon; a browser-free smoke proves serving + security + merge semantics deterministically, leaving only visual parity to a manual pass |

## Log

- **2026-07-19** — **Task 2: whitepaper.** Wrote `docs/whitepaper.md` (~2.6k words), Bitcoin-whitepaper-styled (abstract, 12 numbered sections, conclusion, references): mainline-invisible search over existing BEPs (§2), signed identity + infohash-preserving records (§3), the three isolated search layers L/S/D (§4–6), RIBLT set reconciliation (§7), the signed SNAGG Aggregate B-tree + PPMI pointer (§8), deny-by-default spam resistance (§9), the operator-vs-daemon capability split (§10), and companion/crawler discovery (§11). An accuracy critic fact-checked every claim against the code: **9 major claims verified accurate; 1 MED + 4 minor corrected before commit.** The MED: §5 originally described the sn_search opt-in as a receive-side query-carried token — the real mechanism is a **send-side compile-time gate** (a `PeerToken` mintable only from an observed advertisement; `SendExtension` requires one → vanilla peers get zero frames), and inbound queries are deliberately NOT gated on the sender having advertised (a responder answers by its own policy). Also corrected: §8 "prefix query without downloading the whole tree" (the shipping path fetches the whole companion torrent; page-level partial reads are format-latent, not wired), §4 privacy wording (name keywords still publish unless suppressed), §10 "downgrade honoured immediately on the wire" (inbound gating is live, but there's no re-announce-on-change), the peer_announce timing (a message after the handshake, not inside it), and reference [9] (Lei Yang et al., *Practical Rateless Set Reconciliation*, SIGCOMM 2024).
- **2026-07-19** — **Task 1: fully functional web client.** Built the embedded web UI (`internal/httpapi/web/`) from a stub (18-line placeholder) into a real vanilla-JS SPA at feature parity with the CLI + native GUI — **no build step, no framework, no CDN**, native ES modules served by the daemon via the frozen `go:embed` contract. Preceded by a 7-agent design workflow that mapped the exact API contract → `docs/web-client-design.md` (commit 4db59f5). Structure: `index.html` shell + `static/{app,api,util}.js` + `static/pages/{downloads,search,status,companion,settings}.js` + one theme-aware (`prefers-color-scheme`) mobile-first `style.css`. Five tabs matching the GUI order; only the visible tab polls (Downloads 1s / Status 2s / Companion 3s); tab state in the URL hash. **`api.js` centralizes the two load-bearing contract rules:** non-2xx bodies are read as PLAIN TEXT → `ApiError{status}` (never `JSON.parse`), and writes are same-origin JSON so the daemon's CSRF guard passes with no auth token. **The L/S/D search layers are rendered as three strictly-separate card groups — never merged, sorted, or deduped** (the isolation discipline, same as `gui/search.go`); each block's score formatted by its real type (L/D float, S int); inline `swarm.error`/`dht.error` (HTTP 200) render as a status line, not a throw. Full parity: add-magnet, pause/resume/remove(+forget, two-step confirm), indexing toggle, per-file priority, confirm/flag, status health, publisher keywords, aggregate, reputation, companion follow/unfollow/refresh (429 throttle shown as informational), rate-limit/queue/capabilities merge-PATCH (send only changed fields, repopulate from the clamped echo), About. Verified: a Go embed-manifest test (every module embedded + index.html loads the entry), `node --check` on all 8 modules, and `scripts/smoke-web.sh` — **21 live checks against a real daemon** (serving/404, every read endpoint, the CSRF guard [loopback 200 vs cross-origin 403], PATCH merge + `share_local` 9→2 clamp, plain-text error shape). `scripts/dod-webclient.sh` (3 checks wrapping the 21). Both binaries rebuilt (web assets embed into the CLI). A 9-area adversarial review (field-name/contract, score types, never-merge, XSS, error handling, poll-lifecycle, merge-PATCH, null-safety, timestamps) **cleared 8 and found 1 MED, fixed:** the polling pages assigned the `setInterval` AFTER `await refresh()`, so a tab switch that called `stop()` during the initial in-flight fetch created an orphaned interval that polled forever (unbounded growth on rapid switching) — fixed with a `stopped` flag checked after the await in `start()` (downloads/status/companion). Task decisions T1-1…T1-4 below.
- **2026-07-19** — **Slice 13 (`cmd/dht-smoke` rebuilt — the §6 exit-code fix).** Re-created the standalone live-DHT smoke tool that the rebuild never restored (behavioral reference on `legacy-snapshot`), clean against the CURRENT API (`dhtschema.SaltForKeyword`/`KeywordValue`, `AnacrolixPutter.Put`/`Getter.Get`). It bootstraps a DHT server, then under a FRESH EPHEMERAL identity (never the user's real key) Puts a synthetic keyword value over BEP-44 and Gets it back; `-stress N` adds N concurrent Puts + a latency/success-rate summary. **The §6 fix, isolated in the pure predicate `stressFailedHard(sum)`:** an all-failed stress phase (`total>0 && ok==0`) is now a HARD failure that returns exit 1 — the legacy tool logged that as a warning and still exited 0, so a fully dead DHT path reported PASS. Exit contract documented in `doc.go` + `-h` (description + flags + examples, per the flat-command help rule). Tested: `stressFailedHard` truth table + a real 2-node loopback-DHT smoke round-trip (Put→Get on node A against a shared hub, `-short`-skipped). dod-slice13 now 12/12. Only the netem `testbed` timeout remains of the ops tail. Slice decision S13-6 below.
- **2026-07-19** — **Slice 12 (BEP-51 crawler worker-pool — core).** Built `internal/dhtindex/crawler.go`: a bounded, deterministic breadth-first walk of the DHT that samples infohashes from each node via the shipped `SampleInfohashes` primitive and expands its frontier from the neighbours those nodes return. Rides the standard BEP-51 verb (no new verb/bit/port). **Control is pure Go** — the loop, visit-once dedup, and every bound are code (production-architecture-rules: no LLM in a control loop). Politeness/finiteness bounds: `Workers` concurrent samples, `MaxFrontier` total-visit cap, `MaxInfohashes` stop-expanding target, `PerQuery` per-sample timeout; a cancelled ctx short-circuits every worker. The crawler ONLY discovers infohashes → streams them to a sink; it never fetches metainfo/admits/indexes/downloads (that policy-laden step stays above it, behind fail-closed admission). The sampler is abstracted behind a `sampleFunc` so the worker-pool is unit-tested against a deterministic in-memory DHT graph (discover+expand, cycle-dedup, frontier cap, infohash ceiling, error counting, ctx-cancel, bad-neighbour skip) — 7 tests, race-clean, no live network. `NewCrawler(server, sink, opts, log)` binds it to a live `dht.Server`. **Remaining tail:** the user-facing `swartznet crawl` CLI consumer (bootstrap → seed frontier → CrawlOnce → report) + `--help`/DoD, then the `cmd/dht-smoke` rebuild + netem timeout. Lib-only core; ship behavior unchanged.
- **2026-07-19** — **Slice 12 (aggregate distribution orchestration — the deferred live path).** Wired the cross-publisher distribution that Slice-12-core deferred, keeping the ship default `LayerDMode=legacy`. Two narrow ports on the `aggregatePPMI` backend: **`TreePublisher`** (distribute-on-refresh) and **`TreeResolver`** (resolve-on-lookup). `Refresh` now rebuilds the SNAGG tree, snapshots `(tree,fp,publisher)` under `mu`, then **off-lock** wraps+seeds the tree as a companion torrent and puts the PPMI pointer (commit = the tree fingerprint). `Lookup` for a **non-self** indexer routes through the resolver (PPMI pointer → companion fetch → `OpenVerifiedTree` commit-check), and **degrades to no-hits — never a query error — when the resolver is absent, the publisher is unknown, or the transport fails** (one unreachable publisher must not fail the whole query). Live engine adapters (`internal/engine/aggregate_dist.go`) compose already-tested primitives (`WrapSnaggTorrent`/`OpenVerifiedTree` + `SeedMetaInfo`/`FetchCompanionTorrent` + `PutPPMI`/`GetPPMI`) behind narrow seeder/fetcher/putter/getter interfaces, so the glue is unit-testable with fakes; the publisher **drops the previous snapshot's seed** each refresh so seeds don't accumulate. `layerd.go` wires the **resolver on the read side** (getter-only) and the **publisher on the write side** (putter-only) via a `DistributableBackend` type-assert that legacy backends don't satisfy (no-op → inert by default); `composite` forwards `SetDistribution` to its aggregate secondary and already merges its `Lookup`. Backend-level distribute→resolve e2e through the real wrap/open/commit seam + engine-adapter unit tests (fakes for the network, real on-disk SNAGG round-trip); the timing-sensitive 2-engine-over-real-DHT version stays deferred to `wirecompat/scenarios` (local-only, like the companion e2e). **A 7-risk adversarial review found 1 HIGH + 1 LOW, both fixed + pinned (the other 5 risk areas cleared with evidence):** (HIGH, S12-9) the read-side aggregate backend is built with `pub=ZERO`, so a **self**-lookup was misclassified as cross-publisher and sent through `FetchCompanionTorrent(ownIH)` — whose deferred drop tears down the node's OWN aggregate seed and whose hardcoded `DataDir` return path (self-seed lives in `CompanionDir`) yields zero self-hits; fixed by having the resolver **skip the node's own pubkey** (self-hits already come from Layer L). (LOW, S12-10) retract-to-empty left the last seed + pointer alive; fixed with a `RetractTree` port method that drops the held seed (pointer expires on TTL). 20/20 Slice-12 DoD (+2 new suites, +2 regression tests for the fixes) + cumulative; `go test -race` clean; both binaries rebuilt. This closes the last Slice-12 deferral; the BEP-51 crawler worker-pool + `cmd/dht-smoke` rebuild remain the only opt-in/ops tail. Slice decisions S12-6…S12-10 below.
- **2026-07-19** — **Slice 13 (hardening pass).** Confirmed the CI merge gate (`test.yml`, PR→main) runs the full deterministic wire-compat suite under `-race` — every `contracts/*` golden vector, future-bit tolerance, reject-code-2 — plus `gofmt -s`/`go vet`/`go mod tidy` with the Fyne C deps installed, while the timing-sensitive `wirecompat/scenarios` stay local-only. The pass **caught + fixed real gate breakers** (4 `gofmt` violations + a `go vet` error introduced in Slices 10–12). Reconciled `docs/07` (oldest-hit eviction, sharding reserved — was "MUST shard") and re-verified `docs/06` reject-code-2, the per-peer token bucket, the `int(bleveScore*1000)` rank, and the `ledongthuc/pdf` BSD-3-Clause attribution. `dod-slice13.sh` (10 checks) mirrors the gate locally; 243 cumulative binary DoD checks; `go test -race` clean. Deferred: the in-memory `VanillaPeer` CI double, a rebuilt `cmd/dht-smoke`, the testbed timeout, and the Slice-12 DHT distribution + crawler. Slice decisions S13-1…S13-4 above.
- **2026-07-09** — Phases 0–3 executed and committed (`8e687ab` SPEC §1–7, `96b109c` provisional §0, `ee54afb` ARCHITECTURE+PLAN, `26a897e` provenance records). Paused at the Phase-3/4 human gate.
- **2026-07-17** — State re-verified (working tree clean, `legacy-snapshot` intact, no code drift since the spec snapshot); this consolidated decision log created; gate re-presented to the author.
- **2026-07-17** — **Gate cleared.** Author confirmed SPEC §0 (all five items) and approved ARCHITECTURE.md + PLAN.md. Phase 4 begins: the legacy Go tree is removed from the rebuild branch (preserved on `legacy-snapshot`), and Slice 0 implementation starts.
- **2026-07-17** — **Slice 0 built.** Contracts extracted from legacy by a 4-agent workflow (exact strings ledger in `docs/rebuild/` provenance); implementation + full test suite landed; 37/37 binary DoD checks pass; `go test -race` clean; adversarially reviewed before commit. Slice decisions S0-1…S0-13 above.
- **2026-07-18** — **Slice 1 built.** Identity contracts extracted by a 3-agent workflow; `internal/identity` + daemon/httpapi/CLI wiring landed; 19/19 Slice-1 + 37/37 Slice-0 binary DoD checks pass; race-clean; adversarially reviewed before commit. Slice decisions S1-1…S1-10 above.
- **2026-07-18** — **Slice 2 built.** Engine contracts extracted by a 5-agent workflow (137KB ledger); `contracts/bencode` + `internal/engine` + wirecompat harness + HTTP/CLI surface landed; the `serve` scaffold deleted and folded into `add`; 15/15 Slice-2 + 19/19 + 37/37 cumulative binary DoD checks pass, including a real two-binary loopback magnet transfer; race-clean incl. the transfer scenario; adversarially reviewed before commit. Slice decisions S2-1…S2-13 above.
- **2026-07-18** — **Slice 3 built.** Signing contracts extracted by a 3-agent workflow (golden vector byte-verified against actual legacy output); `contracts/sign` + `internal/signing` + engine create/verify-at-add + `cmd create` landed; 19/19 Slice-3 + cumulative binary DoD checks pass; race-clean; adversarially reviewed before commit. Slice decisions S3-1…S3-9 above.
- **2026-07-18** — **Slice 4 built.** Layer L contracts extracted by a 5-agent workflow (117KB ledger); leaf packages (`contracts/token`, `internal/indexer` + extractors) built by an implementation workflow, then wired (engine ingestion + hourly rescan + Forget, `searchmux`, `/search`+`/index/stats`, CLI `search`/`index`). Driving the real binary caught a genuine multi-file over-read bug (fixed via a size-bounded `ReadSeekerAt`); the 4-lens review caught a `-race` regression on the rescan interval and a restore-ordering race — both fixed and pinned. 18/18 Slice-4 + 108 cumulative binary DoD checks; race-clean. Slice decisions S4-1…S4-9 above.
- **2026-07-19** — **Slice 12 built (core).** The Aggregate index behind the swappable `RecordBackend` seam — opt-in, off by default (**ship default stays `LayerDMode=legacy`**). Frozen contracts: `contracts/snagg` (`b0a24cd`) = the byte-exact signed SNAGG B-tree (6-byte magic, bencoded records, MIN-KEY separators, 162-byte signed trailer, deterministic build, `SHA256(concat EncodeRecord)` fingerprint = the PPMI commit, trailer-verify-before-trust, hostile-tree-guarded `Find`, `VerifyFingerprint`; reuses `contracts/record`; golden-pinned) and `contracts/dhtschema.PPMIValue` + `PPMISalt=SHA256("snet.index")` (`958b009`, golden-pinned). Offline `swartznet aggregate build|inspect|find` (`e1e926e`) = sign+pack JSONL → signed SNAGG (mode 0644, `--pow-bits` refused >40 exit 2), inspect (integrity gate), find `--verify`. Live seam (`d571da7`): `aggregatePPMI` (records → in-memory signed SNAGG tree → authenticated self-lookup) + `composite` (dual-write legacy+aggregate, legacy-then-aggregate dual-read merged+deduped; primary authoritative, secondary logged-never-fatal); `NewBackend` gains a signer param + the two new modes; `config.Validate` accepts `{legacy,composite,aggregatePPMI}`. **DoD proven: composite dual-write → LEGACY-ONLY read still returns the hit** (migration is adapter-selection, no data loss); a broken secondary never breaks the primary. Deny-by-default admission + `seeds.json`→`MarkSeeded` (incl. the §6 "3 fresh Sybils don't clear the bar" fix) were already delivered in Slice 5. Every Slice-12 DoD bullet is satisfied. 16/16 Slice-12 + 233 cumulative binary DoD checks; `go test -race` clean (27 pkgs). **DEFERRED (opt-in, off by default, non-load-bearing):** the DHT PPMI publish/resolve distribution (cross-publisher aggregate discovery) and the BEP-51 crawler worker-pool. Slice decisions S12-1…S12-5 above.
- **2026-07-19** — **Slice 11 built.** The native Fyne GUI (the FIRST GUI binary). `internal/gui` (Fyne v2.7.3) + `cmd/swartznet-gui` present Downloads/Search/Status/Companion/Settings as PURE PRESENTATION over the same `daemon.Daemon`. The search fan-out is unified behind a new **`Daemon.Search`** (the mux is now built unconditionally as `d.mux`); the Search tab renders per-layer **L/S/D cards** from the shared `searchmux.Result` and never merges them (deletes the legacy §5 GUI-private reconciliation), and Confirm/Flag route through the shared `d.Confirm`/`d.Flag`. The About dialog now states **Apache-2.0** for first-party code (the §6 fix — legacy claimed MIT), version/license from one build-stamped source. Poll goroutines mutate widgets only via `fyne.Do` and stop on ctx cancel; `App.onClose` owns the single Daemon lifecycle. Fyne builds + tests headlessly here (CGo + OpenGL present; the `fyne.io/fyne/v2/test` driver). 6-agent extraction ledger `docs/rebuild/slice11-gui-ledger.md`; the focused 3-lens review returned **0 findings**. `./scripts/build-gui.sh dev` builds `dist/swartznet-gui-dev-linux-amd64` (46 MB). 8/8 Slice-11 + 217 cumulative binary DoD checks; `go test -race` clean (0 data races across 26 packages incl. the GUI). Slice decisions S11-1…S11-4 above.
- **2026-07-19** — **Slice 10 built.** The companion content-index (BEP-46 pointer pattern). New `internal/companion` (the gzip-JSON `CompanionIndex` codec with bounded decompression + format/version refuse; the corpus builder; the single-file trackerless `.torrent` wrapper; the `Publisher` [empty=failure, `lastRefresh` only on success, ≤1h ticker]; the `Subscriber` + worker) talks to the DHT/engine ONLY through narrow ports and consumes the Slice-9 BEP-46 pointer primitive. The subscriber closes all three legacy §6 defects — **rejects a snapshot not authored by the followed publisher**, **stamps `SignedBy` = the publisher** on imports, **dedups on `GeneratedAt`** — and the engine `FetchCompanionTorrent` is fail-closed on the untrusted infohash (1 file, ≤32 MiB, safe name). Daemon: independent publisher/subscriber legs (all failures non-fatal), teardown before the index, a persisted atomic follow file; new `GET /companion` + `POST /companion/{refresh,follow,unfollow}` and a new `swartznet companion` CLI (+ gated `add --regtest`). **Headline DoD verified: a REAL two-engine e2e — node A publishes+seeds, node B fetches over BitTorrent through the fail-closed engine, verifies A's authorship, and imports stamped `SignedBy=A`, then dedups.** The 4-lens review (12 agents) found **4 real defects** — a HIGH self-referential feedback loop (seeded companion torrents were auto-indexed back into the corpus + leaked their filenames to Layer D) and three MED engine/goroutine leak paths — all fixed (a distinct non-indexed `companion` engine handle that `autoIndex` skips, drop-previous-seed on the publisher, drop-after-fetch on the subscriber, pointer-infohash dedup-before-fetch) + pinned (3 candidates refuted). 21/21 Slice-10 + 207 cumulative binary DoD checks; `go test -race` clean (0 data races across 25 packages, incl. the real-transfer scenario). Slice decisions S10-1…S10-7 above.
- **2026-07-19** — **Slice 9 built.** Layer D: the BEP-44 mutable keyword index behind the swappable `RecordBackend` seam. New `contracts/dhtschema` (the frozen `KeywordValue` wire — anacrolix bencode, ≤1000-byte pre-unmarshal cap, verbatim salt; golden + bep44 re-marshal-identity + legacy read-back gates). New `internal/dhtindex`: the `RecordBackend` port with the shipping `legacyKeyword` backend, the `Publisher` worker (55m throttle + oldest-hit eviction, driving the backend), the `Lookup` read side (most-distinctive token, parallel reputation-gated scored merge), the shared fail-closed `checkPutStats` beneath `AnacrolixPutter`/`Getter`, and the BEP-46 pointer + BEP-51 `SampleInfohashes` primitives. Engine publish-on-`GotInfo` (NAME tokens only) + retract, with the read side alive leech-only and the write side gated on `--no-index`/`--no-dht-publish` (the privacy cascade); `searchmux` third concurrent layer; httpapi `/search` dht block + `GET /publish` (§5.9 inline error, zero-import law); the `crawl-probe` CLI + `--no-dht-publish`; config `LayerDMode`/`MinIndexerScore`/`PublisherPath`. **Headline DoD verified: a real 2-node DHT cluster round-trips — node A publishes, node B (learning A's pubkey only) resolves A's infohash over BEP-44 — plus the Exp=2h negative guard, the zero-node fail-closed guard, and vanilla-bep44 readability.** The 4-lens review (10 agents) found **1 confirmed defect** — a MED anti-spam hole where one hostile indexer repeating an infohash in its own value faked multi-indexer consensus (→ per-indexer source dedup in `Lookup`, S9-8) — fixed + pinned (3 other candidates refuted as documented/unreachable). 19/19 Slice-9 + 188 cumulative binary DoD checks; `go test -race` clean (0 data races across 24 packages). Slice decisions S9-1…S9-8 above.
- **2026-07-19** — **Slice 8 built.** RIBLT set-reconciliation + the signed-record substrate. Frozen contracts `contracts/riblt` (FNV-1a-64 key, SplitMix64 membership cycle, peel decoder — golden-pinned), `contracts/record` (ElementID excludes pow/sig for dedup, sig message includes pow — golden-pinned), and the `contracts/ltepwire` sync codec (msg 4–8 — golden-pinned). `internal/swarmsearch` gains the `RecordCache`, the `SyncSession` state machine (downward budgets, semantic-size byte accounting, index-desync handling), the multi-batch pump, and the `StartSync` initiator; the engine mints records on `GotInfo` + wires the cache + re-enables bit 9 (masks back to `0x2ED`/`0x2FD` — the S7-7 re-gate fulfilled). **Headline DoD verified: two peers reconcile a 250-record symmetric difference to the union, multi-batch.** 6-agent extraction (`docs/rebuild/slice8-riblt-ledger.md`); the 4-lens review (17 agents) found **12 real defects** — all from the async goroutine-per-frame dispatch reordering/re-firing sync frames (my synchronous test harness masked them): out-of-order-batch false-abort (→ reorder-tolerant `ApplySymbols`), premature convergence + re-fire duplication (→ 256-symbol floor + `done` flag + once-guard), a session-cap TOCTOU, an unthrottled `sync_begin`, and an order-slice leak — all fixed + pinned. 11/11 Slice-8 + 169 cumulative binary DoD checks; `go test -race` clean (0 data races across 22 packages). Slice decisions S8-1…S8-9 above.
- **2026-07-19** — **Slice 7 built.** Layer S: the sn_search BEP-10 (LTEP) peer-wire extension — the first WIRE slice, with mainline compatibility as the hard constraint. `contracts/ltepwire` gains the query/result/reject/peer_announce codec (golden vectors); `internal/swarmsearch` (Handler, PeerToken-gated transport seam, banman, token-bucket, anti-spoof query fan-out) never imports Bleve or the torrent package; the engine wires it through the 4 anacrolix LTEP callbacks with off-read-loop dual-256-semaphore dispatch; `searchmux`/`httpapi`/`daemon` add the swarm fan-in (§5.9 inline error); `wirecompat` gains a raw-socket `MiniPeer` + the vanilla-silence and real-wire-query scenarios. Behavioral extraction by a 6-agent workflow (52KB ledger in `docs/rebuild/slice7-layers-ledger.md`). The 4-lens review (13 agents) found **3 confirmed defects** — a bit-9 over-advertisement ban-trap (re-gated off, S7-7), an asked-set flood/false-ban vector (per-peer consume + grace window, S7-8), and a non-UTF-8 name-truncation empties bug (S7-9) — all fixed + pinned. THE mainline-compat gate is green: a vanilla peer receives ZERO sn_search frames over real loopback while a capable peer gets the peer_announce; a MiniPeer queries the engine over the real wire and gets indexed hits. 11/11 Slice-7 + 158 cumulative binary DoD checks; `go test -race` clean (0 data races across 20 packages incl. the wire scenarios). Slice decisions S7-1…S7-9 above.
- **2026-07-19** — **Slice 6 built.** Capability mask: the single producer `contracts/ltepwire.Announced(Sharing, RuntimeFacts)` (frozen bit constants + golden vectors) plus the type split (operator `Sharing` bits 0–3 vs daemon `RuntimeFacts` bits 4–9), wired through `Engine.ServicesMask()`, the httpapi `ServicesReporter`/`Capabilities` collaborators (`GET`/`PATCH`/`POST /capabilities`, `PATCH` mutates only `Sharing`), and the live `/aggregate` + `/capabilities` `services` readout. Closes two §6 defects fully (static-`0x2ED`; "save sharing" clobbering Publisher — now structurally impossible) and the producer half of the third (downgrades honored; the wire half is Slice 7). Behavioral extraction by a 6-agent workflow (ledger in `docs/rebuild/slice6-capability-ledger.md`); a 4-lens adversarial review (recomputing every hex by hand) returned **0 findings**. 17/17 Slice-6 + 130 cumulative binary DoD checks; race-clean (0 data races across 19 packages). Slice decisions S6-1…S6-8 above.
- **2026-07-18** — **Slice 5 built.** Spam-resistance: leaf packages `internal/reputation` (frozen FNV-64a Bloom v1 + golden vector, Bayesian reputation, source-attribution LRU), `internal/trust` (publisher allowlist), `internal/admission` (deny-by-default) built by a leaf workflow, then wired — engine `loadSpamResistance`/`Checkpoint`/`ConfirmHit`/`FlagHit`, the **one shared** `daemon.Confirm`/`daemon.Flag` path, httpapi `POST /confirm`·`/flag`·`GET /aggregate` + `/status` Bloom/reputation blocks, CLI `trust`/`confirm`/`flag`. Driving the real binary caught an offline `trust add` clean-install dir bug; the 4-lens review (10 agents, 6 raw → 3 confirmed after adversarial verify) caught a **HIGH** concurrent-checkpoint `.tmp` corruption race (→ unique `os.CreateTemp` + `ckptMu` + `Close` joins the checkpoint goroutine), a **MED** corrupt-`trust.json` fail-OPEN that could silently demote trusted publishers (→ fail-CLOSED `trust-unavailable`), and a **LOW** `--help` gap — all fixed and pinned. 22/22 Slice-5 + 130 cumulative binary DoD checks; race-clean (0 data races across 18 packages). Slice decisions S5-1…S5-9 above.
